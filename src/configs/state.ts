import { State } from '@schemas/state'

import * as CURRENT_VERSION from '@engine/saves/versions/v1'
export * as CURRENT_VERSION from '@engine/saves/versions/v1'

export const DEFAULT_SAVE: State = CURRENT_VERSION.DefaultSave
